% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ducklake.R
\name{ducklake_register_parquet}
\alias{ducklake_register_parquet}
\title{Register existing Parquet files in a DuckLake table}
\usage{
ducklake_register_parquet(con, table, parquet_files, create_table = TRUE)
}
\arguments{
\item{con}{DuckDB connection with DuckLake attached.}

\item{table}{Target table name (optionally qualified, e.g., "lake.variants").}

\item{parquet_files}{Character vector of Parquet file paths/URIs.}

\item{create_table}{Logical, create the table if it doesn't exist. Default: TRUE.
When TRUE, schema is inferred from the first Parquet file.}
}
\value{
Invisibly returns the number of files registered.
}
\description{
Adds Parquet files that already exist (from prior ETL) to a DuckLake table.
This is a catalog-only operation; data files are not copied or moved.
}
\details{
This function uses DuckLake's \code{ducklake_add_data_files()} to register
external Parquet files in the catalog. The files must already exist and
have a schema compatible with the target table.
}
\examples{
\dontrun{
# Register a Parquet file created by vcf_to_parquet_duckdb()
ducklake_register_parquet(con, "variants", "s3://bucket/variants.parquet")
}
}

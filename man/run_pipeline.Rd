% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Run an External Executable Pipeline}
\usage{
run_pipeline(
  stages,
  stdin = NULL,
  stdout = NULL,
  stderr = NULL,
  error_on_status = TRUE
)
}
\arguments{
\item{stages}{A pipeline stage or a non-empty list of objects created by
\code{\link[=pipeline_stage]{pipeline_stage()}}.}

\item{stdin}{Optional input file for the first stage. \code{NULL} inherits the R
process's standard input.}

\item{stdout}{Optional output file for the last stage. \code{NULL} inherits the R
process's standard output. Binary output should always be directed to a
file or handled by the final executable itself.}

\item{stderr}{Optional shared error-log file. \code{NULL} inherits the R
process's standard error.}

\item{error_on_status}{Whether to raise an error when any stage exits with a
non-zero status.}
}
\value{
A data frame with one row per stage and columns \code{stage}, \code{command},
\code{status}, and \code{signal}.
}
\description{
Connects the standard output of each stage directly to the standard input
of the next stage using operating-system pipes. Arguments are passed
directly to each executable; no shell command is constructed or evaluated.
}
\details{
This function is intended for streaming groups such as an aligner writing
SAM to standard output followed by \verb{samtools sort}. File-dependent steps,
including FastDup and \verb{samtools index}, should be run as subsequent
one-stage pipelines.
}
\examples{
output <- tempfile(fileext = ".txt")
result <- run_pipeline(
  list(
    pipeline_stage("printf", c("alpha\\\\nbeta\\\\n")),
    pipeline_stage("grep", "beta")
  ),
  stdout = output
)
readLines(output)
result

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ducklake.R
\name{ducklake_write_variants}
\alias{ducklake_write_variants}
\title{Write variants into a DuckLake table}
\usage{
ducklake_write_variants(
  con,
  table,
  vcf_path,
  method = c("parquet", "direct"),
  threads = parallel::detectCores(),
  compression = "zstd",
  row_group_size = 100000L,
  partition_by = NULL,
  overwrite = FALSE,
  bcf_reader_extension = NULL,
  region = NULL,
  ...
)
}
\arguments{
\item{con}{DuckDB connection with DuckLake attached.}

\item{table}{Target table name (optionally qualified, e.g., "ducklake.variants").}

\item{vcf_path}{Path/URI to VCF/BCF.}

\item{method}{"parquet" (default) to convert to Parquet then ingest, or "direct" to insert via \code{bcf_read}.}

\item{threads}{Threads for conversion (passed to \code{vcf_to_parquet}).}

\item{compression}{Parquet compression codec (default "zstd").}

\item{row_group_size}{Parquet row group size (default 100000).}

\item{partition_by}{Optional character vector of columns to partition by when creating a new table.}

\item{overwrite}{Logical, drop and recreate the table.}

\item{bcf_reader_extension}{Optional path to bcf_reader.duckdb_extension (needed for \code{method = "direct"} if not installed).}

\item{region}{Optional region string for \code{bcf_read} when using \code{method = "direct"}.}

\item{...}{Additional arguments passed to \code{vcf_to_parquet}.}
}
\value{
Invisible NULL.
}
\description{
Write variants into a DuckLake table
}

diff --git a/src/markdup/read_ends.h b/src/markdup/read_ends.h
index 7dbae55..7c43e33 100644
--- a/src/markdup/read_ends.h
+++ b/src/markdup/read_ends.h
@@ -31,12 +31,9 @@ struct PhysicalLocation {
      * that the x and y positions are ints, not shorts thus, they do not
      * overflow within a HiSeqX tile.
      */
-    int16_t tile = -1;
-    // int32_t x = -1;
-    // int32_t y = -1;
-    // This is a bug in picard Markduplicates, because some tile coordinates exceede the range of int16_t
-    int16_t x = -1;
-    int16_t y = -1;
+    int32_t tile = -1;
+    int32_t x = -1;
+    int32_t y = -1;
 };
 
 /* read ends，picard ReadEndsForMarkDuplicates*/
diff --git a/src/markdup/read_name_parser.h b/src/markdup/read_name_parser.h
index d153523..315c616 100644
--- a/src/markdup/read_name_parser.h
+++ b/src/markdup/read_name_parser.h
@@ -129,7 +129,7 @@ struct ReadNameParser {
                     }
                     return false;
                 }
-                loc->tile = (int16_t)tmpLocationFields[0];
+                loc->tile = tmpLocationFields[0];
                 loc->x = tmpLocationFields[1];
                 loc->y = tmpLocationFields[2];
                 return true;

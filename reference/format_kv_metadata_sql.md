# Format metadata for DuckDB KV_METADATA clause

Format metadata for DuckDB KV_METADATA clause

## Usage

``` r
format_kv_metadata_sql(metadata)
```

## Arguments

- metadata:

  Named list of key-value pairs

## Value

Character string with KV_METADATA SQL clause

# Get FastDup Version

Runs the bundled FastDup executable and returns its upstream version.

## Usage

``` r
fastdup_version()
```

## Value

A character string containing the FastDup version, or `NA` when the
executable is unavailable on the current build.

## Examples

``` r
fastdup_version()
#> [1] "1.0.0"
```

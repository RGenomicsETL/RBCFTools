# Get the source directory for bcf_reader extension in the package

Get the source directory for bcf_reader extension in the package

## Usage

``` r
bcf_reader_source_dir()
```

## Value

Character string with the path to the extension source directory

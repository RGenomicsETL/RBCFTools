# Load the DuckLake extension

Load the DuckLake extension

## Usage

``` r
ducklake_load(con, install = TRUE)
```

## Arguments

- con:

  A DuckDB connection.

- install:

  Logical, attempt `INSTALL ducklake` before loading. Defaults to TRUE.

## Value

The connection (invisibly).
